{
  "name": "interactive-rating",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "./interactive-rating/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/interactive-rating"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nrwl/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "interactive-rating:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "interactive-rating:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "interactive-rating:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nrwl/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "interactive-rating:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "interactive-rating:build:development"
        },
        "production": {
          "buildTarget": "interactive-rating:build:production"
        }
      }
    },
    "test": {
      "executor": "@nrwl/vite:test",
      "outputs": ["coverage/interactive-rating"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "../coverage/interactive-rating"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["./interactive-rating/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
